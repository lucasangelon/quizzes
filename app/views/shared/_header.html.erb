<div class="sticky">
    <nav class="top-bar" data-topbar role="navigation">
        <ul class="title-area">
            <li class="name">
                <h1><a href="#">Quizzes</a></h1>
            </li>
            
            <li class="toggle-topbar menu-icon"><a href="#"><span>Menu</span></a></li>
        </ul>
    
    
        <section class="top-bar-section">
            <ul class="right">
                <% if logged_in? %>
                    <li>
                        <%= link_to "Edit Profile", edit_user_path(current_user) %>
                    </li>
                    
                    <li class="divider"></li>
                    
                    <li>
                        <%= link_to "Quizzes Main Website", root_path %>
                    </li>
                    
                    <li>
                        <%= link_to "Logout", logout_path, method: :delete %>
                    </li>
                <% else %>
                    <li>
                        <%= link_to "Quizzes Main Website", root_path %>
                    </li>
                <% end %>
            </ul>
            
            <p class="left short-padding quizzes-orange"><%= Time.zone.now.to_formatted_s(:long) %></p>
        </section>
    </nav>
</div>